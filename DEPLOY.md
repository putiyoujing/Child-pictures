# 部署指南

本指南将帮助您将儿童识字小报生成器部署到GitHub和Cloudflare Pages。

## 🚀 GitHub部署

### 1. 创建GitHub仓库

1. 登录 [GitHub](https://github.com)
2. 点击右上角的 "+" 按钮，选择 "New repository"
3. 填写仓库信息：
   - Repository name: `Child-pictures`
   - Description: `儿童识字小报生成器 - 基于AI的儿童识字学习工具`
   - 选择 Public 或 Private
   - 勾选 "Add a README file"（如果还没有README）
   - 勾选 "Add .gitignore"（选择Node.js）
   - 勾选 "Choose a license"（选择MIT）

4. 点击 "Create repository"

### 2. 上传项目文件

#### 方法一：使用GitHub Desktop（推荐）

1. 下载并安装 [GitHub Desktop](https://desktop.github.com/)
2. 点击 "File" → "Add Local Repository"
3. 选择项目文件夹
4. 在Repository name中输入 `Child-pictures`
5. 点击 "Create Repository"
6. 点击 "Publish repository"，选择您的GitHub账号
7. 勾选 "Keep this code private"（如需私有）
8. 点击 "Publish repository"

#### 方法二：使用Git命令行

1. 打开项目文件夹，运行以下命令：

```bash
# 初始化Git仓库
git init

# 添加所有文件
git add .

# 第一次提交
git commit -m "Initial commit: 儿童识字小报生成器"

# 添加远程仓库
git remote add origin https://github.com/putiyoujing/Child-pictures.git

# 推送到GitHub
git branch -M main
git push -u origin main
```

### 3. 配置GitHub Pages（可选）

如果只是想通过GitHub Pages托管，可以：

1. 进入GitHub仓库页面
2. 点击 "Settings" 选项卡
3. 在左侧菜单中找到 "Pages"
4. 在 "Source" 下选择 "Deploy from a branch"
5. "Branch" 选择 "main"，文件夹选择 "/ (root)"
6. 点击 "Save"
7. 等待几分钟，您的网站将在 `https://putiyoujing.github.io/Child-pictures` 上线

## ☁️ Cloudflare Pages部署（推荐）

Cloudflare Pages提供更快的访问速度和更好的CDN服务。

### 1. 准备GitHub仓库

确保您的代码已经推送到GitHub（参考上面的步骤）。

### 2. 登录Cloudflare

1. 访问 [Cloudflare Dashboard](https://dash.cloudflare.com/)
2. 登录您的Cloudflare账号

### 3. 连接GitHub账户

1. 在左侧菜单中点击 "Workers & Pages"
2. 点击 "Create application"
3. 选择 "Pages" 选项卡
4. 点击 "Connect to Git"

### 4. 选择仓库

1. 在弹出的授权页面，点击 "Connect to GitHub"
2. 安装Cloudflare应用（如果第一次使用）
3. 授权后，在仓库列表中找到 `Child-pictures`
4. 点击 "Begin setup"

### 5. 配置构建设置

1. **构建设置**：
   - Framework preset: `None`
   - Build command: 留空（静态网站无需构建）
   - Root directory: 留空

2. **环境变量**（可选）：
   - 如需设置环境变量，可以在此添加

3. 点击 "Save and Deploy"

### 6. 部署完成

1. Cloudflare Pages会自动部署您的网站
2. 部署完成后，您会获得一个类似 `https://child-pictures.pages.dev` 的URL
3. 您可以在自定义域名中设置自己的域名（如需要）

## 🔧 常见问题

### 1. CORS错误

**问题**：直接打开HTML文件会出现CORS错误

**解决**：
- 必须通过HTTP服务器访问，不能直接双击打开HTML文件
- 使用提供的服务器脚本或部署到在线平台

### 2. API密钥安全

**建议**：
- 不要在公开的代码中硬编码API密钥
- 本地开发时，API密钥会保存在浏览器的localStorage中
- 部署到线上时，用户需要自己输入API密钥

### 3. 图片生成失败

**检查项**：
- API密钥是否正确
- 网络连接是否正常
- API服务是否可用
- 查看浏览器控制台的错误信息

### 4. 自定义域名（Cloudflare）

1. 在Cloudflare Pages项目设置中，点击 "Custom domains"
2. 输入您的域名，如 `child-pictures.example.com`
3. 按照提示在您的域名DNS设置中添加CNAME记录
4. 等待DNS生效（通常需要几分钟到几小时）

## 📌 部署后的维护

### 更新网站

当您修改代码后：

1. **本地测试**：确保修改没有问题
2. **提交更改**：
   ```bash
   git add .
   git commit -m "更新内容说明"
   git push
   ```
3. **自动部署**：Cloudflare Pages会自动检测到更改并重新部署

### 版本管理

建议使用语义化版本号：
- 主版本号：不兼容的API修改
- 次版本号：向下兼容的功能性新增
- 修订号：向下兼容的问题修正

## 🎉 完成部署

恭喜！您的儿童识字小报生成器已经成功部署！

- **GitHub仓库**：https://github.com/putiyoujing/Child-pictures
- **Cloudflare Pages**：https://child-pictures.pages.dev

现在您可以分享给朋友们使用了！

## 📞 获取帮助

如果遇到问题：

1. 查看 [GitHub Issues](https://github.com/putiyoujing/Child-pictures/issues)
2. 提交新的Issue描述您的问题
3. 或联系项目维护者

---

祝您使用愉快！🎊